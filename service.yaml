apiVersion: v1
kind: Service

metadata:
  name: aws-java-service
  namespace: devops-demo

spec:

  type: LoadBalancer

  selector:
    app: aws-java-app

  ports:

    - protocol: TCP
      port: 80
      targetPort: 8080
